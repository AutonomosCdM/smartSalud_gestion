# Ollama URL for the backend to connect
# The path '/ollama' will be redirected to the specified backend URL
OLLAMA_BASE_URL='http://localhost:11434'

OPENAI_API_BASE_URL=''
OPENAI_API_KEY=''

# Google API Key for Gemini (required for smartDoc)
# Get your key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=''

# AUTOMATIC1111_BASE_URL="http://localhost:7860"

# Session secret key - REQUIRED for production
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
WEBUI_SECRET_KEY=''

# For production, you should only need one host as
# fastapi serves the svelte-kit built frontend and backend from the same host and port.
# SECURITY: Never use '*' in production!
# Example: CORS_ALLOW_ORIGIN='http://localhost:5173;http://localhost:8080'
CORS_ALLOW_ORIGIN='http://localhost:8080'

# For production you should set this to match the proxy configuration
# SECURITY: Never use '*' in production!
FORWARDED_ALLOW_IPS='127.0.0.1'

# DO NOT TRACK
SCARF_NO_ANALYTICS=true
DO_NOT_TRACK=true
ANONYMIZED_TELEMETRY=false